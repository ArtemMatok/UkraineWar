@{
    List<Object> menuItems = new List<Object>();

    menuItems.Add(new
    {
        text = "Україна",
        url = "/Ukraine",
        //items = new List<object>()
        //{
        //    new{text = "Open"},
        //    new{text = "Save"},
        //    new{text = "Exit"},
        //}
        iconCss = "fa-solid fa-folder-closed",
    });
    menuItems.Add(new
    {
        text = "росія",
        url = "/rus",
        iconCss = "fa-solid fa-arrow-right-arrow-left"
    });
}

 <style>
        /* CSS стилі для початкового стану */
        .big-bold-text {
            font-size: 24px;
            font-weight: bold;
              color: #fff; /* Зелений колір тексту (#008000) */
            transition: font-size 0.3s ease; /* Плавний перехід розміру шрифту протягом 0.3 секунди */
        }

        /* CSS стилі при наведенні */
        .big-bold-text:hover {
            font-size: 32px; /* Розмір шрифту при наведенні миші */
            color: yellow;
        }
        .big-text {
            font-size: 24px;
            font-weight: bold;
              color: #fff; /* Зелений колір тексту (#008000) */
            transition: font-size 0.3s ease; /* Плавний перехід розміру шрифту протягом 0.3 секунди */
        }

        /* CSS стилі при наведенні */
        .big-text:hover {
            font-size: 32px; /* Розмір шрифту при наведенні миші */
            color: red;
            
        }
        
    </style>
<ejs-sidebar id="sidebar" width="290px">
    <e-content-template>
        <div style="display: flex; flex-direction: column; align-items: center; justify-content: center; height: 100%;">
           <a class="big-bold-text" asp-controller="Ukraine" asp-action="Index">Ukraine</a>
           <a class="big-text" asp-controller="Russia" asp-action="Index">Russia</a>
        </div>
    </e-content-template>
</ejs-sidebar>
